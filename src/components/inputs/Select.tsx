export default function Select() {
    return (
        <select name="" disabled id="" className="py-2 px-3 rounded-lg border-2 border-gray-500">
            <option value="">Active Orders (0)</option>
        </select>
    );
}
